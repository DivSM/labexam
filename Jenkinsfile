pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'Jenkins build completed'
            }
        }
        stage('test') {
            steps {
                echo 'Jenkins testing done'
                echo 'complete'
            }
        }
    }
}
